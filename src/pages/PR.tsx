import { Megaphone } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

const PR = () => {
  return (
    <PlaceholderPage
      title="PR"
      description="Manage your public relations with press release templates, media contacts, and coverage tracking."
      icon={Megaphone}
    />
  );
};

export default PR;
