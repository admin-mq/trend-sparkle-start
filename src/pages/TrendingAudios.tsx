import { Music } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

const TrendingAudios = () => {
  return (
    <PlaceholderPage
      title="Trending Audios"
      description="Find viral sounds and music tracks across platforms to boost your content's reach and engagement."
      icon={Music}
    />
  );
};

export default TrendingAudios;
