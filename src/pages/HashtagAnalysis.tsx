import { Hash } from "lucide-react";
import { PlaceholderPage } from "./PlaceholderPage";

const HashtagAnalysis = () => {
  return (
    <PlaceholderPage
      title="Hashtag Analysis"
      description="Discover trending hashtags, analyze their performance, and find the perfect tags for your content strategy."
      icon={Hash}
    />
  );
};

export default HashtagAnalysis;
